/*import React, { useState } from "react"

class Data {

	constructor(userMeasurements) {
		const [measurements, setMeasurements] = useState({});
		const [formData, setFormData] = useState({});
		setMeasurements({
			length: userMeasurements["length"],
			width: userMeasurements["width"]
		});
		alert("Length " + measurements["length"] + "Width " + measurements["width"]);
    }

	*//*const setExternalMeasurements = (userMeasurements) => {
		setMeasurements({
			length: userMeasurements["length"],
			width: userMeasurements["width"]
		});
		alert("Length " + measurements["length"] + "Width " + measurements["width"]);
	}*//*
}

export default Data;*/